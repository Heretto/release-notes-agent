# hop-core (sibling repo — clone https://github.com/heretto/hop-core next to this repo
# and keep it on a main that includes hop_core.dita; the [dita] extra pulls lxml).
#
# SINGLE SOURCE OF TRUTH POLICY:
# hop-core owns the versions of its own dependencies — auth, crypto, oauth, rate-limiting,
# lxml, pydantic-settings, email-validator, python-multipart, itsdangerous. Those are NOT
# re-pinned here; they resolve transitively from hop-core so there is exactly one version
# constraint for each and no drift/conflict. Only packages this app imports directly are
# listed below. CI runs `pip check` to catch any incompatibility.
-e ../hop-core[dita]

# Core FastAPI and async support
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0

# Database and ORM
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.13.3

# Redis and Celery
redis==5.0.1
celery==5.3.4

# AI and API integrations
google-generativeai==0.3.2
anthropic==0.39.0
openai==2.26.0
httpx==0.25.1

# XML processing (defusedxml used directly; lxml provided transitively by hop-core[dita])
defusedxml==0.7.1

# Environment and config
python-dotenv==1.0.0
pydantic==2.9.2

# Email
aiosmtplib==3.0.2
