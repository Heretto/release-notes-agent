-r requirements.txt

# hop-doctor's compose check needs PyYAML, which the [doctor] extra supplies. pip unions
# this with the [dita] extra pinned in requirements.txt — keep the tag on both lines in sync.
hop-core[doctor] @ git+https://github.com/Heretto/hop-core.git@v0.1.3

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.1
faker==20.1.0

# Development tools
black==23.11.0
flake8==6.1.0
mypy==1.7.1
pre-commit==3.5.0